# Quality Score

Track quality by product area and architectural layer so agents can prioritize the weakest parts of the system.

## Suggested Scale

- `A`: strong coverage, stable behavior, clear docs, low operational risk.
- `B`: acceptable but still has known gaps.
- `C`: works but needs targeted hardening.
- `D`: fragile or underspecified.

## Current Snapshot

| Area | Score | Why | Next Step |
| --- | --- | --- | --- |
| Product surface | C | CLI and registry can import catalogs, crawl and publish well-known catalogs, onboard MCP/A2A/Skill/OpenAPI artifacts, list and remove entries, export catalogs, check remote health, search, browse, explore, return federation referrals, run bounded score-ranked `federation=auto` upstream merge with opaque cross-registry page tokens, validate search/explore request shape plus browse query parameters and federation modes, paginate local search/list/admin review/admin audit responses, and manage remote admin APIs. Combined, CLI-only, server-only, and public Go SDK surfaces are available. Public health checks, public browse, remote `ardctl browse`, `pkg/client`, and local CLI listing share documented unauthenticated or deterministic workflows where appropriate. Local search now has a documented first-release ranking contract: relevance-only scores, descending score order, and stable catalog tie-breakers. Browse/list filters support equality, exclusion, containment, timestamp boundaries, `AND`/`OR`, and parenthesized groups. Admin lifecycle status can hide inactive entries from public discovery and well-known catalog publication, review decisions can record audit reasons, admin mutation audit events are queryable and hash-chain verifiable, and an MVP ingestion policy can deny, pend, require multiple reviewers, require trust metadata before persistence, or run as a `verify catalog` preflight without persistence. The Go SDK now covers both discovery and token-protected admin management routes, with documented pre-1.0 compatibility boundaries and external-module coverage for all public client methods plus helper/error types. Release packaging can produce versioned Linux/macOS archives for all three binaries with embedded version metadata, an SPDX SBOM, and SHA-256 checksums. Tagged releases publish archives and generate signed GitHub artifact attestations for provenance plus SBOM. | Cut the first public tag only after a final API/SDK compatibility pass. |
| Architecture docs | B | `docs/ARCHITECTURE.md` now describes the Go/Cobra/Gin/GORM/Postgres stack, package boundaries, and container distribution shape. | Keep adapter, API, storage, and deployment decisions updated as public SDK boundaries and release automation emerge. |
| Testing | B | Unit tests, external Go client import checks, Postgres integration tests, build checks, workflow invariant checks, upstream ARD conformance runs, GitHub Actions CI, a manual/weekly E2E workflow, `make test-e2e`, and `make test-compose` cover core ingestion/search/admin/artifact-onboarding, local search ranking, well-known catalog publication, review decision audit reasons and multi-reviewer thresholds, trust metadata policy gates, pagination including admin audit pagination, federation ranking/cross-registry page-token/request correlation, artifact-fetch request correlation, SDK discovery/admin calls, all public Go client methods from an external module, browse/list filter operators and grouped expressions, CI/E2E/release workflow shape, and deployment paths, including live MCP, Skill, and OpenAPI artifacts plus a checked-in A2A fixture. | Monitor scheduled E2E flakes, upstream artifact drift, and rate limits before promoting live-artifact E2E into a required pull-request gate. |
| Observability | B | Health check exists and includes build version, commit, build date, and active entry count. `ardctl health` and `ardctl metrics` expose public operational checks from the CLI. HTTP responses include request IDs and W3C `traceparent`, JSON access logs include request IDs plus trace IDs and span IDs, admin audit events include request IDs for correlation, `federation=auto` propagates request IDs and trace context to upstream registry calls, outbound catalog/artifact/source-digest/attestation-digest/provenance-digest fetches propagate request IDs and trace context when present, and `/metrics` exposes Prometheus-style uptime, request, in-flight, HTTP duration histogram, goroutine, heap, and GC metrics. Trace export/backends and dashboards are not implemented. | Add trace export/backend integration and a documented local observability workflow. |
| Security | C | Validation enforces schema-aligned catalog root and host fields, `urn:air:`, media type syntax, value/reference exclusivity, duplicate identifiers within a catalog, absolute HTTP(S) URLs, `updatedAt` date-time format, scalar metadata values, basic catalog shape, host metadata, minimal `trustManifest` structure, `identityType` enum values plus identity-shape checks, schema-aligned known-field checks for trust metadata, `trustSchema`/signature shape, attestation/provenance structure, and entry HTTP(S)/SPIFFE/`did:web` `trustManifest.identity` trust-domain alignment with the `urn:air:` publisher. URL artifacts can be pinned and verified with the documented `trustManifest.sourceDigest` extension, and strict catalog verification can require every URL-delivered entry to carry a pinned source digest. Attestation documents can be fetched and verified against pinned `trustManifest.attestations[].digest` values, with a strict mode requiring every attestation to carry a digest. HTTP(S) provenance sources can be fetched and verified against pinned `trustManifest.provenance[].sourceDigest` values, with a strict mode requiring every HTTP(S) provenance `sourceId` to carry a digest. Detached compact JWS `trustManifest.signature` values can be verified against explicit Ed25519 trust anchors supplied in ard-native format, local JWKS OKP/Ed25519 format, or explicit HTTPS remote JWKS URLs whose host matches the entry trust domain, and strict verification can require every catalog entry to carry a verifiable signature. Admin API routes are disabled by default and guarded by bearer tokens when enabled, with optional role-scoped reader, publisher, reviewer, operator, and admin token files that reload for runtime rotation. Lifecycle status prevents disabled or pending entries from appearing in public discovery. Ingestion policy can deny, pend, require `trustManifest`, URL source digests, and JWS signatures before persistence, and the same policy can be preflighted through `ard verify catalog`. Admin mutation events are logged with request IDs and hash-chain verification, but externally anchored audit logs, auditor trust/freshness verification, provenance lineage truth verification, DID/SPIFFE/certificate/OIDC automatic key discovery, and signed-claim truth verification are still pending. | Design DID/SPIFFE/certificate key discovery before broader network exposure. |
