# Quality Score

Track quality by product area and architectural layer so agents can prioritize the weakest parts of the system.

## Suggested Scale

- `A`: strong coverage, stable behavior, clear docs, low operational risk.
- `B`: acceptable but still has known gaps.
- `C`: works but needs targeted hardening.
- `D`: fragile or underspecified.

## Current Snapshot

| Area | Score | Why | Next Step |
| --- | --- | --- | --- |
| Product surface | C | CLI and registry can import catalogs, crawl and publish well-known catalogs, onboard MCP/A2A/Skill/OpenAPI artifacts, list and remove entries, export catalogs, search, browse, explore, return federation referrals, run bounded score-ranked `federation=auto` upstream merge with opaque cross-registry page tokens, validate search/explore request shape plus browse query parameters and federation modes, paginate local search/list/admin review/admin audit responses, and manage remote admin APIs. Combined, CLI-only, server-only, and public Go SDK surfaces are available. Public browse, remote `ardctl browse`, `pkg/client`, and local CLI listing share deterministic filtering on common spec fields, tags, capabilities, metadata keys, and whitelisted ordering. Browse/list filters support equality, exclusion, containment, timestamp boundaries, `AND`/`OR`, and parenthesized groups. Admin lifecycle status can hide inactive entries from public discovery and well-known catalog publication, review decisions can record audit reasons, admin mutation audit events are queryable and hash-chain verifiable, and an MVP ingestion policy can deny or pend new and updated entries for review with optional multi-reviewer approval thresholds. The Go SDK now covers both discovery and token-protected admin management routes, with documented pre-1.0 compatibility boundaries. Release packaging can produce versioned Linux/macOS archives for all three binaries with an SPDX SBOM and SHA-256 checksums. Tagged releases publish archives and generate signed GitHub artifact attestations for provenance plus SBOM. | Cut the first public tag only after a final API/SDK compatibility pass. |
| Architecture docs | B | `docs/ARCHITECTURE.md` now describes the Go/Cobra/Gin/GORM/Postgres stack, package boundaries, and container distribution shape. | Keep adapter, API, storage, and deployment decisions updated as public SDK boundaries and release automation emerge. |
| Testing | B | Unit tests, external Go client import checks, Postgres integration tests, build checks, workflow invariant checks, upstream ARD conformance runs, GitHub Actions CI, `make test-e2e`, and `make test-compose` cover core ingestion/search/admin/artifact-onboarding, well-known catalog publication, review decision audit reasons and multi-reviewer thresholds, pagination including admin audit pagination, federation ranking/cross-registry page-token/request correlation, artifact-fetch request correlation, SDK discovery/admin calls, browse/list filter operators and grouped expressions, release workflow shape, and deployment paths, including live MCP, Skill, and OpenAPI artifacts plus a checked-in A2A fixture. | Consider an optional scheduled E2E workflow once external artifact availability and rate limits are better understood. |
| Observability | B | Health check exists. HTTP responses include request IDs and W3C `traceparent`, JSON access logs include request IDs plus trace IDs and span IDs, admin audit events include request IDs for correlation, `federation=auto` propagates request IDs and trace context to upstream registry calls, outbound catalog/artifact/source-digest fetches propagate request IDs and trace context when present, and `/metrics` exposes Prometheus-style uptime, request, in-flight, HTTP duration histogram, goroutine, heap, and GC metrics. Trace export/backends and dashboards are not implemented. | Add trace export/backend integration and a documented local observability workflow. |
| Security | C | Validation enforces schema-aligned catalog root and host fields, `urn:air:`, media type syntax, value/reference exclusivity, duplicate identifiers within a catalog, absolute HTTP(S) URLs, `updatedAt` date-time format, scalar metadata values, basic catalog shape, host metadata, minimal `trustManifest` structure, `identityType` enum values, schema-aligned known-field checks for trust metadata, `trustSchema`/signature shape, attestation/provenance structure, and entry HTTP(S) `trustManifest.identity` host alignment with the `urn:air:` publisher. URL artifacts can be pinned and verified with the documented `trustManifest.sourceDigest` extension. Admin API routes are disabled by default and guarded by bearer tokens when enabled, with optional role-scoped reader, publisher, reviewer, operator, and admin token files that reload for runtime rotation. Lifecycle status prevents disabled or pending entries from appearing in public discovery. Ingestion policy can deny or pend entries before persistence. Admin mutation events are logged with request IDs and hash-chain verification, but externally anchored audit logs, attestation content verification, detached signatures, and identity proof verification are still pending. | Add signature/identity verification design before broader network exposure. |
