# Quality Score

Track quality by product area and architectural layer so agents can prioritize the weakest parts of the system.

## Suggested Scale

- `A`: strong coverage, stable behavior, clear docs, low operational risk.
- `B`: acceptable but still has known gaps.
- `C`: works but needs targeted hardening.
- `D`: fragile or underspecified.

## Current Snapshot

| Area | Score | Why | Next Step |
| --- | --- | --- | --- |
| Product surface | C | CLI and registry can import catalogs, crawl well-known catalogs, onboard MCP/A2A/Skill/OpenAPI artifacts, list and remove entries, export catalogs, search, browse, explore, return federation referrals, run bounded score-ranked `federation=auto` upstream merge without leaking local-only page tokens as federated cursors, paginate local search/list/admin review/admin audit responses, and manage remote admin APIs. Combined, CLI-only, and server-only entrypoints are available. Admin lifecycle status can hide inactive entries from public discovery, review decisions can record audit reasons, admin mutation audit events are queryable and hash-chain verifiable, and an MVP ingestion policy can deny or pend new and updated entries for review. Multi-step approval workflows and true cross-registry federation cursors are still missing. | Define multi-step approval workflows and design true federated cursors if pagination across upstream registries becomes required. |
| Architecture docs | B | `docs/ARCHITECTURE.md` now describes the Go/Cobra/Gin/GORM/Postgres stack, package boundaries, and container distribution shape. | Keep adapter, API, storage, and deployment decisions updated as public SDK boundaries and release automation emerge. |
| Testing | B | Unit tests, Postgres integration tests, build checks, upstream ARD conformance runs, GitHub Actions CI, `make test-e2e`, and `make test-compose` cover core ingestion/search/admin/artifact-onboarding, review decision audit reasons, pagination including admin audit pagination, federation ranking/page-token/request correlation, artifact-fetch request correlation, and deployment paths, including live MCP, Skill, and OpenAPI artifacts plus a checked-in A2A fixture. | Consider an optional scheduled E2E workflow once external artifact availability and rate limits are better understood. |
| Observability | B | Health check exists. HTTP responses include request IDs and W3C `traceparent`, JSON access logs include request IDs plus trace IDs and span IDs, admin audit events include request IDs for correlation, `federation=auto` propagates request IDs and trace context to upstream registry calls, outbound catalog/artifact/source-digest fetches propagate request IDs and trace context when present, and `/metrics` exposes Prometheus-style uptime, request, in-flight, HTTP duration histogram, goroutine, heap, and GC metrics. Trace export/backends and dashboards are not implemented. | Add trace export/backend integration and a documented local observability workflow. |
| Security | C | Validation enforces `urn:air:`, value/reference exclusivity, URLs, basic catalog shape, host metadata, minimal `trustManifest` structure, `identityType` enum values, attestation/provenance structure, and entry HTTP(S) `trustManifest.identity` host alignment with the `urn:air:` publisher. URL artifacts can be pinned and verified with `trustManifest.sourceDigest`. Admin API routes are disabled by default and guarded by bearer tokens when enabled, with optional role-scoped reader, publisher, reviewer, operator, and admin token files that reload for runtime rotation. Lifecycle status prevents disabled or pending entries from appearing in public discovery. Ingestion policy can deny or pend entries before persistence. Admin mutation events are logged with request IDs and hash-chain verification, but externally anchored audit logs, attestation content verification, detached signatures, and identity proof verification are still pending. | Add signature/identity verification design before broader network exposure. |
