## [2026-06-20 22:10] | Task: Add Real CI

### User Request

> Continue building the Go/Cobra/GORM/Gin/Postgres ARD implementation with real
> verification, open-source project norms, and milestone commits.

### Changes

- Added GitHub Actions CI for pushes to `main` and pull requests.
- CI runs:
  - `make fmt-check`
  - `make test`
  - `make build`
  - `make test-integration`
- Added a Postgres 16 CI service for integration tests.
- Added `scripts/check-fmt.sh` and `make fmt-check`.
- Updated `scripts/test-integration.sh` to reuse `ARD_TEST_DATABASE_URL` when provided,
  while preserving Docker-backed local integration runs.
- Updated README, repository collaboration guidance, and quality score.

### Design Notes

The repository previously avoided placeholder CI. This workflow is tied to the actual Go
stack and exercises Postgres-backed integration tests, so it is now useful as an
open-source quality gate instead of a cosmetic badge.

### Verification

- Passed: `make fmt-check`
- Passed: `make test`
- Passed: `make build`
- Passed: `make test-integration` with the local Docker-backed Postgres path.
- Passed: `ARD_TEST_DATABASE_URL=<postgres-service-url> make test-integration` with an
  externally started Postgres container, matching the CI service-database path.
